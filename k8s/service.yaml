apiVersion: v1
kind: Service
metadata:
  name: furniture-shop-service
spec:
  selector:
    app: furniture-shop
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP  # Or LoadBalancer if external access needed